 @* instead of repeating the code like above for all the column header we will make partial view and supply the column header dynamiccally*@

 @await Html.PartialAsync("_TableColumnHeader",new ViewDataDictionary(ViewData)
 // new ViewDatadicitionary(ViewData) copies the existing viewData from the parent view and adds below key value pair to it and is send as viewBag 
  // to the partial view 
 {
 {"DisplayName","Name"},
 {"QueryName",nameof(PersonResponse.PersonName)}
 });